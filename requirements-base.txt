# requirements-base.txt
# Used by: api, celery_beat, flower
# Does NOT include sentence-transformers or torch
# Estimated size contribution: ~400 MB

# ── Core API ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
gunicorn>=22.0.0
python-multipart>=0.0.9
pydantic>=2.7.0

# ── Auth ──────────────────────────────────────────────────
bcrypt==4.0.1
python-jose[cryptography]>=3.3.0

# ── LangChain / LangGraph ─────────────────────────────────
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-qdrant>=0.1.0
langchain-text-splitters>=0.2.0
langchain-community>=0.2.0
langchain-experimental>=0.0.60
langgraph>=0.1.0
langsmith>=0.1.0
langgraph-checkpoint-postgres>=1.0.0,<3.0.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# ── Task queue ────────────────────────────────────────────
celery[redis]>=5.3.0
flower>=2.0.0

# ── Vector DB ─────────────────────────────────────────────
qdrant-client==1.13.0

# ── Sparse retrieval ──────────────────────────────────────
rank-bm25>=0.2.2
flashrank>=0.2.9          # lightweight reranker — available in both api and worker images

# ── Async DB + cache ──────────────────────────────────────
asyncpg>=0.29.0
redis[hiredis]>=5.0.0

# ── PDF parsing ───────────────────────────────────────────
pypdf>=4.2.0

# ── Observability ─────────────────────────────────────────
prometheus-fastapi-instrumentator>=6.1.0
slowapi>=0.1.9

# ── Utilities ─────────────────────────────────────────────
python-dotenv>=1.0.0
msgpack>=1.0.0          # for deserialising LangGraph checkpoint blobs
httpx>=0.27.0
numpy>=1.26
werkzeug